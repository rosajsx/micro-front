import { colors } from "../colors";

export const dark = {
  background: colors.gray[900],
  text: colors.gray[50],
  textSecondary: colors.gray[50],
  main: colors.orange[500],
  warning: colors.red[900],
  success: colors.green[400],
};
