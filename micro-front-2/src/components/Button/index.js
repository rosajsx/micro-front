import React from "react";
export const Button = () => {
  return (
    <button onClick={() => console.log("clicou no micro front 2")}>
      Micro front 2 button
    </button>
  );
};

export default Button;
